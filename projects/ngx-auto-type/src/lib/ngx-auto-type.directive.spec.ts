import { NgxAutoTypeDirective } from './ngx-auto-type.directive';

describe('NgxAutoTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxAutoTypeDirective();
    expect(directive).toBeTruthy();
  });
});
